# API_KEY is the key to access the API
API_KEY=
# PORT is the port number to run the server
PORT=
# ENVIRONMENT is the environment to run the server (e.g. development, production) and is used for logging
ENVIRONMENT=

